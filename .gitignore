.idea
.vscode
upload
*.exe
*.db
build
# ...but keep the embedded router test fixture (router/web_test.go
# //go:embeds it; a fresh clone fails to compile without it).
!router/web/build/
router/web/build/*
!router/web/build/testbuild/
router/web/build/testbuild/*
!router/web/build/testbuild/index.html
*.db-journal
logs
data
/web/node_modules
cmd.md
.env
/one-api
/bin/
temp
.DS_Store

# Agent / task tracker state (managed out of band by bitx-loop)
.beads/
.bitx/nas-deploy/__pycache__/

# Tool index / agent state (managed out of band)
.bitx/
.codegraph/
__pycache__/
